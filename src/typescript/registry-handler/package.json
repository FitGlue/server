{
  "name": "registry-handler",
  "version": "1.0.0",
  "description": "Returns the FitGlue registry (connections and plugins)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "gcp-build": "npm run build",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "start": "functions-framework --target=registryHandler",
    "dev": "concurrently \"tsc -w\" \"functions-framework --target=registryHandler\""
  },
  "dependencies": {
    "@fitglue/shared": "^1.0.0",
    "@google-cloud/functions-framework": "^3.0.0",
    "@typescript-eslint/eslint-plugin": "^8.53.1",
    "@typescript-eslint/parser": "^8.53.1",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.0.0",
    "concurrently": "^8.0.0",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6"
  },
  "overrides": {
    "@types/node": "^20.0.0"
  }
}
